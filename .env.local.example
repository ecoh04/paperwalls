# Copy this file to .env.local and fill in your real values.
# Never commit .env.local to git — it contains secrets.

# Supabase (database)
# Get these from: Supabase Dashboard → Your Project → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: Supabase service role key (only for server-side admin tasks; keep secret)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Stitch Express (payments – South Africa)
# Get from: https://express.stitch.money – API keys / integration
# If not set, checkout redirects straight to success (for local testing only).
# STITCH_API_URL=https://api.stitch.money/...
# STITCH_API_KEY=your-stitch-api-key

# App URL (for payment redirects and webhooks). Use your live URL in production.
NEXT_PUBLIC_APP_URL=http://localhost:3000
